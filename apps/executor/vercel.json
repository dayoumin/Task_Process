{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile --filter @task-process/executor && pnpm --filter @task-process/executor build",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping install, using buildCommand'",
  "framework": "vite"
}
