{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @task-process/dashboard build",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping install, using buildCommand'",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
