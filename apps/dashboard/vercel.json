{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile --filter @task-process/dashboard && pnpm --filter @task-process/dashboard build",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping install, using buildCommand'",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
