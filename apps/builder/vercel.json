{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile --filter @task-process/builder && pnpm --filter @task-process/builder build",
  "outputDirectory": "dist",
  "installCommand": "echo 'Skipping install, using buildCommand'",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
